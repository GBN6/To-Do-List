const createNewTask = (title, info, date, priority, projectIndex, taskIndex) => {
    return{
        title,
        info, 
        date, 
        priority, 
        projectIndex, 
        taskIndex
    }
}




export default createNewTask;